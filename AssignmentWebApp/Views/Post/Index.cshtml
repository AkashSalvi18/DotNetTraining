@model AssignmentWebApp.Models.PostViewModel

<div class="card">
    <div class="card-body">
        <h3>@Model.Title</h3>
        <h6>By @Model.Author</h6>
        <p>@Model.Body</p>
    </div>
</div>

@await Component.InvokeAsync("Comments", new { postId = Model.Id })
